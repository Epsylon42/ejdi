{
    let line = readline();
    print(line.ord(), "\n");
    let i = 0;
    let break = false;
    while {i < line.len()} && !break {
        if line.at(i) == " " {
            line = line.slice(i, line.len());
            break = true;
        };

        i = i + 1;
    };

    print(line);
}
