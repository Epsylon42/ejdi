let std = require("std");
let range = std.range;

let make_arr = func() {
    let arr = [];
    for i in range(10) {
        arr.push(i)
    }
    arr
};

let arr = make_arr();
print(arr.map(func(x) x + 2), "\n");
